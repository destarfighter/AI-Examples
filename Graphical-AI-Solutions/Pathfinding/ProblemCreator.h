#pragma once
#include "ProblemPackage.h"

namespace ProblemCreator {
	static ProblemDefinition GenerateProblem() {
		// Create a random map
		// select a random destination tile
		// create / return package
	}
	static void DeployNewProblem(ProblemDefinition newProblem) {
		// push update to map
	}
}

